extend type Mutation {
    createProgram(program: ProgramInput): ID
    updateProgram(program: ProgramInput): ID
    deleteProgram(id: ID): ID
}

input ProgramInput {
    id: ID
    name: String
    path: String
}